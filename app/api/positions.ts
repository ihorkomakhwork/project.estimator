import IContainer from 'app/contract/icrontainer';
import type { IController } from '../../lib/lib';
import type { IUserDTO, IUserDAO } from '../contract/idomain';

export default ({ crudService }: IContainer) => ({
    
});
